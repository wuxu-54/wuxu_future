# Platform Channel示例

我们通过一个完整的实战项目来学习 Platform Channel。这个项目会创建一个 Flutter 插件，功能是**获取电池电量**（MethodChannel）和**实时监听电池状态变化**（EventChannel）。你将看到 Dart、Android (Kotlin)、iOS (Swift) 三端的全部代码，以及如何在应用中使用。

---

## 1. 项目初始化

打开终端，创建一个 Flutter 插件项目：

```bash
flutter create --org com.example --template=plugin battery_plugin
```

项目结构：

```
battery_plugin/
  lib/battery_plugin.dart           # Dart 公共 API
  android/src/main/kotlin/.../      # Android 原生代码
  ios/Classes/                      # iOS 原生代码
  example/                          # 示例 Flutter 应用
```

我们将修改以下文件：

- `lib/battery_plugin.dart`
- `android/src/main/kotlin/.../BatteryPlugin.kt`
- `ios/Classes/BatteryPlugin.swift`

---

## 2. Dart 端 API

打开 `lib/battery_plugin.dart`，编写对外的 API：

```dart
import 'dart:async';
import 'package:flutter/services.dart';

class BatteryPlugin {
  // MethodChannel：获取电量
  static const MethodChannel _methodChannel =
      MethodChannel('com.example.battery_plugin/method');

  // EventChannel：监听电池状态
  static const EventChannel _eventChannel =
      EventChannel('com.example.battery_plugin/event');

  /// 获取当前电池电量（0–100）
  static Future<int> getBatteryLevel() async {
    try {
      final level = await _methodChannel.invokeMethod<int>('getBatteryLevel');
      return level ?? 0;
    } on PlatformException catch (e) {
      print("获取电量失败: ${e.message}");
      return -1; // 错误时返回 -1
    }
  }

  /// 返回电池状态的广播流
  /// 状态值: "charging", "discharging", "full", "unknown"
  static Stream<String> get batteryStateStream {
    return _eventChannel
        .receiveBroadcastStream()
        .map((event) => event as String);
  }
}
```

**要点：**

- MethodChannel 调用 `invokeMethod('getBatteryLevel')`，返回一个 `Future`。
- EventChannel 的 `receiveBroadcastStream()` 返回一个 `Stream`，用 `map` 转型。
- 错误处理：捕获 `PlatformException`。

---

## 3. Android 原生实现

打开 `android/src/main/kotlin/com/example/battery_plugin/BatteryPlugin.kt`，完整代码如下：

```kotlin
package com.example.battery_plugin

import android.content.BroadcastReceiver
import android.content.Context
import android.content.Intent
import android.content.IntentFilter
import android.os.BatteryManager
import io.flutter.embedding.engine.plugins.FlutterPlugin
import io.flutter.plugin.common.EventChannel
import io.flutter.plugin.common.MethodCall
import io.flutter.plugin.common.MethodChannel

class BatteryPlugin : FlutterPlugin, MethodChannel.MethodCallHandler, EventChannel.StreamHandler {

    private lateinit var methodChannel: MethodChannel
    private lateinit var eventChannel: EventChannel
    private var applicationContext: Context? = null
    private var eventSink: EventChannel.EventSink? = null
    private var batteryReceiver: BroadcastReceiver? = null

    companion object {
        private const val METHOD_CHANNEL = "com.example.battery_plugin/method"
        private const val EVENT_CHANNEL = "com.example.battery_plugin/event"
    }

    // ================= FlutterPlugin 接口 =================
    override fun onAttachedToEngine(binding: FlutterPlugin.FlutterPluginBinding) {
        applicationContext = binding.applicationContext

        // 初始化 MethodChannel
        methodChannel = MethodChannel(binding.binaryMessenger, METHOD_CHANNEL)
        methodChannel.setMethodCallHandler(this)

        // 初始化 EventChannel
        eventChannel = EventChannel(binding.binaryMessenger, EVENT_CHANNEL)
        eventChannel.setStreamHandler(this)
    }

    override fun onDetachedFromEngine(binding: FlutterPlugin.FlutterPluginBinding) {
        methodChannel.setMethodCallHandler(null)
        eventChannel.setStreamHandler(null)
        applicationContext = null
    }

    // ================= MethodCallHandler 实现 =================
    override fun onMethodCall(call: MethodCall, result: MethodChannel.Result) {
        if (call.method == "getBatteryLevel") {
            val level = getBatteryLevel()
            if (level >= 0) {
                result.success(level)
            } else {
                result.error("UNAVAILABLE", "无法获取电量", null)
            }
        } else {
            result.notImplemented()
        }
    }

    private fun getBatteryLevel(): Int {
        val context = applicationContext ?: return -1
        val batteryManager = context.getSystemService(Context.BATTERY_SERVICE) as BatteryManager
        return batteryManager.getIntProperty(BatteryManager.BATTERY_PROPERTY_CAPACITY)
    }

    // ================= EventChannel.StreamHandler 实现 =================
    override fun onListen(arguments: Any?, events: EventChannel.EventSink?) {
        eventSink = events
        registerReceiver()
    }

    override fun onCancel(arguments: Any?) {
        unregisterReceiver()
        eventSink = null
    }

    private fun registerReceiver() {
        val context = applicationContext ?: return
        batteryReceiver = object : BroadcastReceiver() {
            override fun onReceive(context: Context?, intent: Intent?) {
                val status = intent?.getIntExtra(BatteryManager.EXTRA_STATUS, -1) ?: -1
                val state = when (status) {
                    BatteryManager.BATTERY_STATUS_CHARGING -> "charging"
                    BatteryManager.BATTERY_STATUS_DISCHARGING -> "discharging"
                    BatteryManager.BATTERY_STATUS_FULL -> "full"
                    else -> "unknown"
                }
                eventSink?.success(state)
            }
        }
        val filter = IntentFilter(Intent.ACTION_BATTERY_CHANGED)
        context.registerReceiver(batteryReceiver, filter)
    }

    private fun unregisterReceiver() {
        batteryReceiver?.let { receiver ->
            applicationContext?.unregisterReceiver(receiver)
        }
        batteryReceiver = null
    }
}
```

**关键点解释：**

- `onAttachedToEngine` 中获取 `applicationContext`，不使用 Activity，避免内存泄漏。
- `getBatteryLevel()` 通过 `BatteryManager` 获取电量，必须 API 21+。
- 电池状态监听使用 `BroadcastReceiver`，在 `onListen` 中注册，在 `onCancel` 中注销，防止泄漏。
- 状态变化时调用 `eventSink.success(state)`，向 Dart 端发送字符串。
- 错误通过 `result.error()` 返回，Dart 端会收到 `PlatformException`。

---

## 4. iOS 原生实现

打开 `ios/Classes/BatteryPlugin.swift`：

```swift
import Flutter
import UIKit

public class BatteryPlugin: NSObject, FlutterPlugin, FlutterStreamHandler {

    private var eventSink: FlutterEventSink?
    private var methodChannel: FlutterMethodChannel?
    private var eventChannel: FlutterEventChannel?

    // ================= FlutterPlugin 注册 =================
    public static func register(with registrar: FlutterPluginRegistrar) {
        let instance = BatteryPlugin()

        // MethodChannel
        let methodChannel = FlutterMethodChannel(
            name: "com.example.battery_plugin/method",
            binaryMessenger: registrar.messenger()
        )
        instance.methodChannel = methodChannel
        registrar.addMethodCallDelegate(instance, channel: methodChannel)

        // EventChannel
        let eventChannel = FlutterEventChannel(
            name: "com.example.battery_plugin/event",
            binaryMessenger: registrar.messenger()
        )
        instance.eventChannel = eventChannel
        eventChannel.setStreamHandler(instance)
    }

    // ================= MethodCallHandler =================
    public func handle(_ call: FlutterMethodCall, result: @escaping FlutterResult) {
        switch call.method {
        case "getBatteryLevel":
            let level = getBatteryLevel()
            if level >= 0 {
                result(level)
            } else {
                result(FlutterError(code: "UNAVAILABLE", message: "无法获取电量", details: nil))
            }
        default:
            result(FlutterMethodNotImplemented)
        }
    }

    private func getBatteryLevel() -> Int {
        let device = UIDevice.current
        device.isBatteryMonitoringEnabled = true
        guard device.batteryState != .unknown else {
            return -1
        }
        return Int(device.batteryLevel * 100)
    }

    // ================= FlutterStreamHandler =================
    public func onListen(withArguments arguments: Any?, eventSink events: @escaping FlutterEventSink) -> FlutterError? {
        eventSink = events
        UIDevice.current.isBatteryMonitoringEnabled = true
        NotificationCenter.default.addObserver(
            self,
            selector: #selector(batteryStateDidChange),
            name: UIDevice.batteryStateDidChangeNotification,
            object: nil
        )
        // 发送初始状态
        sendBatteryState()
        return nil
    }

    public func onCancel(withArguments arguments: Any?) -> FlutterError? {
        NotificationCenter.default.removeObserver(self)
        eventSink = nil
        return nil
    }

    @objc private func batteryStateDidChange(notification: Notification) {
        sendBatteryState()
    }

    private func sendBatteryState() {
        let state: String
        switch UIDevice.current.batteryState {
        case .charging:
            state = "charging"
        case .full:
            state = "full"
        case .unplugged:
            state = "discharging"
        default:
            state = "unknown"
        }
        eventSink?(state)
    }
}
```

**关键点解释：**

- iOS 插件必须实现 `FlutterPlugin` 并添加 `register` 静态方法。
- `UIDevice.current.isBatteryMonitoringEnabled = true` 必须开启才能获取电量和监听状态变化。
- 通过 `NotificationCenter` 监听 `UIDevice.batteryStateDidChangeNotification`，取消监听时务必移除。
- `eventSink` 是闭包，直接调用发送事件，注意线程安全（通知回调默认在主线程，安全）。
- 错误处理：`FlutterError`。

---

## 5. 在示例应用中使用插件

打开 `example/lib/main.dart`，编写一个简单的 UI 来展示插件功能：

```dart
import 'dart:async';
import 'package:flutter/material.dart';
import 'package:battery_plugin/battery_plugin.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatefulWidget {
  @override
  _MyAppState createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  int _batteryLevel = 0;
  String _batteryState = 'unknown';
  late StreamSubscription<String> _stateSubscription;

  @override
  void initState() {
    super.initState();
    // 获取一次电量
    BatteryPlugin.getBatteryLevel().then((level) {
      setState(() => _batteryLevel = level);
    });
    // 订阅电池状态变化
    _stateSubscription = BatteryPlugin.batteryStateStream.listen((state) {
      setState(() => _batteryState = state);
    });
  }

  @override
  void dispose() {
    _stateSubscription.cancel(); // 取消订阅，防止内存泄漏
    super.dispose();
  }

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text('电池插件示例')),
        body: Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              Text(
                '当前电量: $_batteryLevel%',
                style: TextStyle(fontSize: 24),
              ),
              SizedBox(height: 20),
              Text(
                '充电状态: $_batteryState',
                style: TextStyle(fontSize: 24, color: Colors.blue),
              ),
              SizedBox(height: 30),
              ElevatedButton(
                onPressed: () async {
                  final level = await BatteryPlugin.getBatteryLevel();
                  setState(() => _batteryLevel = level);
                },
                child: Text('刷新电量'),
              ),
            ],
          ),
        ),
      ),
    );
  }
}
```

**运行效果：**

- 页面显示当前电量百分比和充电状态（charging/discharging/full/unknown）。
- 点击按钮可主动刷新电量。
- 插拔充电器，状态文字会实时变化。

---

## 6. 编译运行

1. 进入插件根目录，确保有设备连接。
2. 运行示例应用：

   ```bash
   cd battery_plugin/example
   flutter run
   ```

真实设备上可以观察到电池状态变化。

---

## 7. 知识点总结

通过这个实战项目，你学到了：

- **MethodChannel** 用于 Dart → 原生的一次性调用，返回 Future。
- **EventChannel** 用于原生 → Dart 的持续数据流，返回 Stream。
- 原生端通过 `BinaryMessenger` 绑定通道，在引擎生命周期内管理资源。
- Android 需实现 `FlutterPlugin`（及 `MethodCallHandler`、`StreamHandler`），在 `onAttachedToEngine` 初始化。
- iOS 需实现 `FlutterPlugin` 并提供 `register` 方法，遵循 `FlutterStreamHandler`。
- 错误处理：原生端使用 `result.error()` 或 `FlutterError`，Dart 端捕获 `PlatformException`。
- 资源释放：EventChannel 必须在 `onCancel` 中解除监听，Activity 或引擎销毁时移除通道 Handler。

现在你已经掌握了 Platform Channel 的核心机制，可以在此基础上开发任何原生功能的 Flutter 插件。
