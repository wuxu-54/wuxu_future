# 开发实记

- 可以使用online建数据库表，并快速生成前后端代码。但注意，使用时需要在菜单配置为本地路径，同时需要修改`sys_permission`表对应的component字段改为正确views下的路径。 还有个特别的点，子菜单，必须将  `是否为路由开关` 打开，否则不会在前端注册进入路由地址，访问不到。
- 自己配置数据字典时，别忘了在字段上添加注解，比如：`@Dict(dicCode = "product_type")`，否则不会返回字典code对应的名称
- 前端配置模糊查询，后端需要在查询接口处添加支持模糊匹配的查询配置：

    ```java
    查询接口：{
        // 自定义查询规则
        Map<String, QueryRuleEnum> customeRuleMap = new HashMap<>(); //主要是这个配置，支持两个参数可以模糊查询
        // 自定义多选的查询规则为：LIKE_WITH_OR
        customeRuleMap.put("partyA", QueryRuleEnum.LIKE_WITH_OR); 
        customeRuleMap.put("partyB", QueryRuleEnum.LIKE_WITH_OR);

        QueryWrapper<CoopAgreementInfo> queryWrapper = QueryGenerator.initQueryWrapper(coopAgreementInfo, req.getParameterMap(),customeRuleMap);
		Page<CoopAgreementInfo> page = new Page<CoopAgreementInfo>(pageNo, pageSize);
		IPage<CoopAgreementInfo> pageList = coopAgreementInfoService.page(page, queryWrapper);
		return Result.OK(pageList);
	}
	```

- Entity中如果设定了默认值，在使用`initQueryWrapper`时，可能会导致作为查询条件了。

    ```java
    @AutoLog(value = "合作协议信息-分页列表查询")
	@Operation(summary="合作协议信息-分页列表查询")
	@GetMapping(value = "/list")
	public Result<IPage<CoopAgreementInfo>> queryPageList(CoopAgreementInfo coopAgreementInfo,
								   @RequestParam(name="pageNo", defaultValue="1") Integer pageNo,
								   @RequestParam(name="pageSize", defaultValue="10") Integer pageSize,
								   HttpServletRequest req) {

		//coopAgreementInfo.setAgreementStatus(null); 
		//coopAgreementInfo.setApproveStatus(null);
		QueryWrapper<CoopAgreementInfo> queryWrapper = QueryGenerator.initQueryWrapper(coopAgreementInfo, req.getParameterMap());
		Page<CoopAgreementInfo> page = new Page<CoopAgreementInfo>(pageNo, pageSize);
		IPage<CoopAgreementInfo> pageList = coopAgreementInfoService.page(page, queryWrapper);
		return R
    }
    

    //解决方法1：去掉默认值
    //解决方法2：coopAgreementInfo.setAgreementStatus(null);   不推荐，后面需要用到此字段做条件过滤就有问题了
    //解决方法3：手动配置，设单独进行查询条件控制，不用jeecgboot动态生成的代码，或在其基础上添加查询条件控制。
    ```

- 账号
  - 有个username字段，这对应前端中的“用户账号”，是唯一的，不能重复，因此使用时要使用这个字段。（如消息推送，框架逻辑是通过username查userId再进行推送）

- online权限问题：online表单中使用关联记录后提示无权限访问 <https://help.jeecg.com/java/online/form/qa>
 	- 问题现象：当用户操作包含关联记录的在线表单时，系统提示「无权限访问（操作）」。
 	- 背景分析
	  1. 当前角色未分配【在线表单】功能模块的菜单访问权限
	  2. 在线表单通过【关联记录】组件引用了其他业务表（以下简称"关联表"），但角色未获得关联表的访问权限
 	- 解决方案，通过配置隐藏路由实现权限隔离，具体操作如下：
	  1. 创建隐藏路由 在菜单管理中创建一个【隐藏路由】（路由菜单选否，隐藏路由选是）
	  2. 在角色权限管理界面，为目标角色授予：
	  	- 主表单的标准访问权限
  		- 关联表的隐藏路由访问权限
