# LangChain API 完整指南（2026年最新稳定版）

**一句话定义**：LangChain是一个**基于Runnable协议的模块化AI应用开发框架**，提供了统一的接口来连接大模型、工具、知识库和记忆系统。截至2026年5月，最新稳定版为**v1.3.0**，已全面重构为以LCEL为核心、LangGraph为编排层的生产级架构。

## 一、整体架构与核心包结构

LangChain采用**三层模块化架构**，严格分离抽象、核心实现和社区集成：

| 包名 | 职责 | 包含内容 |
|------|------|----------|
| `langchain-core` | 基础抽象层 | 定义所有核心接口（Runnable、ChatModel、Tool等）、LCEL实现、通用工具 |
| `langchain` | 核心实现层 | 提供开箱即用的链、代理、记忆和工具实现 |
| `langchain-{provider}` | 独立集成包 | 针对特定服务商的集成（如`langchain-openai`、`langchain-anthropic`） |
| `langchain-community` | 社区贡献层 | 第三方贡献的集成和工具（逐渐迁移至独立包） |

**安装命令**：

```bash
# 核心包
pip install langchain langchain-core

# 常用集成
pip install langchain-openai langchain-community

# 生产级工具链（2026年新增）
pip install langsmith langserve langgraph-platform
```

## 二、核心基础：Runnable协议

所有LangChain组件都实现了**Runnable协议**，这是LangChain的灵魂。它定义了统一的调用接口，让任何组件都可以通过`|`管道符自由组合。

### 2.1 标准调用方法

所有Runnable对象都支持以下5种标准调用方式：

| 方法 | 功能 | 返回值 |
|------|------|--------|
| `invoke(input, config=None)` | 同步调用 | 单个结果 |
| `ainvoke(input, config=None)` | 异步调用 | 单个结果（协程） |
| `stream(input, config=None)` | 同步流式输出 | 结果生成器 |
| `astream(input, config=None)` | 异步流式输出 | 结果异步生成器 |
| `batch(inputs, config=None)` | 批量调用 | 结果列表 |

**示例**：

```python
from langchain_core.runnables import RunnableLambda

# 定义一个简单的Runnable
add_one = RunnableLambda(lambda x: x + 1)

# 各种调用方式
print(add_one.invoke(1))  # 输出: 2
print(add_one.batch([1, 2, 3]))  # 输出: [2, 3, 4]

for token in add_one.stream(5):
    print(token)  # 输出: 6
```

### 2.2 LCEL（LangChain Expression Language）

LCEL是基于Runnable协议的声明式语法，使用`|`管道符连接组件，数据从左到右自动流转。

**基础语法**：

```python
chain = component1 | component2 | component3
result = chain.invoke(input_data)
```

**核心组合原语**：

- **RunnableSequence**：顺序执行（`|`）
- **RunnableParallel**：并行执行（`{}`）
- **RunnableAssign**：向字典中添加新字段
- **RunnableBranch**：条件分支执行

**示例：并行执行**

```python
from langchain_core.runnables import RunnableParallel

chain = RunnableParallel({
    "a": lambda x: x["num"] + 1,
    "b": lambda x: x["num"] * 2
})

print(chain.invoke({"num": 5}))
# 输出: {"a": 6, "b": 10}
```

## 三、模型API

LangChain提供了统一的接口来调用不同厂商的大模型，分为**LLM（文本补全模型）**和**ChatModel（聊天模型）**两大类，目前ChatModel是主流。

### 3.1 ChatModel（聊天模型）

这是最常用的模型接口，支持多轮对话和工具调用。

**核心类**：

- `ChatOpenAI`：OpenAI GPT系列（GPT-4o, GPT-5.5）
- `ChatAnthropic`：Anthropic Claude系列（Claude 3.5, Claude Opus 4.7）
- `ChatGoogleGenerativeAI`：Google Gemini系列（Gemini 3.1 Pro）
- `ChatDeepSeek`：DeepSeek系列（DeepSeek-V4）
- `ChatOllama`：本地Ollama模型
- `init_chat_model()`：通用模型构造器（推荐）

**初始化与基本调用**：

```python
from langchain_openai import ChatOpenAI
from langchain_core.messages import HumanMessage, SystemMessage

# 初始化模型
model = ChatOpenAI(
    model="gpt-4o",
    temperature=0,
    max_tokens=1024,
    api_key="your-api-key"
)

# 基本调用
messages = [
    SystemMessage(content="你是一个有用的助手"),
    HumanMessage(content="你好，介绍一下你自己")
]

response = model.invoke(messages)
print(response.content)
```

**流式输出**：

```python
for chunk in model.stream(messages):
    print(chunk.content, end="", flush=True)
```

### 3.2 工具调用API

所有支持工具调用的ChatModel都实现了`bind_tools()`方法，这是构建智能体的基础。

**定义工具**：

```python
from langchain_core.tools import tool
from pydantic import BaseModel, Field

# 方法1：使用@tool装饰器（推荐）
@tool
def add(a: int, b: int) -> int:
    """将两个整数相加"""
    return a + b

# 方法2：使用Pydantic模型定义
class MultiplyInput(BaseModel):
    """乘法输入参数"""
    a: int = Field(..., description="第一个整数")
    b: int = Field(..., description="第二个整数")

@tool(args_schema=MultiplyInput)
def multiply(a: int, b: int) -> int:
    """将两个整数相乘"""
    return a * b
```

**绑定与调用工具**：

```python
# 绑定工具到模型
model_with_tools = model.bind_tools([add, multiply])

# 调用模型，模型会自动决定是否使用工具
response = model_with_tools.invoke("3加5等于多少？再乘以2是多少？")

# 检查工具调用
if response.tool_calls:
    print("工具调用：", response.tool_calls)
    # 执行工具
    from langgraph.prebuilt import ToolNode
    tool_node = ToolNode([add, multiply])
    tool_results = tool_node.invoke({"messages": [response]})
    print("工具结果：", tool_results)
```

**强制调用特定工具**：

```python
# 强制模型调用multiply工具
model_force_multiply = model.bind_tools(
    [multiply],
    tool_choice={"type": "function", "function": {"name": "multiply"}}
)
```

## 四、提示词API

提示词模板用于将用户输入和系统指令格式化为模型可以理解的消息格式。

### 4.1 ChatPromptTemplate（聊天提示词模板）

这是最常用的提示词模板，支持多角色消息和占位符。

**基本用法**：

```python
from langchain_core.prompts import ChatPromptTemplate

# 创建聊天提示词模板
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个{role}，请用{language}回答问题"),
    ("human", "{question}")
])

# 格式化提示词
formatted_prompt = prompt.format_messages(
    role="数学老师",
    language="中文",
    question="什么是勾股定理？"
)

# 与模型组合成链
chain = prompt | model
response = chain.invoke({
    "role": "数学老师",
    "language": "中文",
    "question": "什么是勾股定理？"
})
```

**高级用法：MessagesPlaceholder**

```python
from langchain_core.prompts import MessagesPlaceholder

# 支持动态消息历史的提示词模板
prompt_with_history = ChatPromptTemplate.from_messages([
    ("system", "你是一个有用的助手"),
    MessagesPlaceholder(variable_name="chat_history"),
    ("human", "{question}")
])
```

## 五、输出解析器API

输出解析器用于将模型的原始输出转换为结构化数据（如字符串、JSON、Pydantic对象）。

### 5.1 常用解析器

| 解析器 | 功能 | 返回值 |
|--------|------|--------|
| `StrOutputParser` | 提取模型输出的文本内容 | 字符串 |
| `JsonOutputParser` | 解析JSON格式的输出 | 字典 |
| `PydanticOutputParser` | 解析为Pydantic模型对象 | Pydantic实例 |

**示例：PydanticOutputParser**

```python
from langchain_core.output_parsers import PydanticOutputParser
from pydantic import BaseModel, Field

class Person(BaseModel):
    name: str = Field(description="人的姓名")
    age: int = Field(description="人的年龄")
    occupation: str = Field(description="人的职业")

parser = PydanticOutputParser(pydantic_object=Person)

prompt = ChatPromptTemplate.from_messages([
    ("system", "请根据用户描述生成一个人物信息，使用以下格式：\n{format_instructions}"),
    ("human", "{description}")
]).partial(format_instructions=parser.get_format_instructions())

chain = prompt | model | parser
person = chain.invoke({"description": "一个30岁的软件工程师，名叫张三"})
print(person.name)  # 输出: 张三
```

### 5.2 结构化输出API（推荐）

对于支持结构化输出的模型（如GPT-4o、Claude 3），推荐使用更简洁的`with_structured_output()`方法：

```python
# 直接让模型输出Pydantic对象
structured_model = model.with_structured_output(Person)
person = structured_model.invoke("一个30岁的软件工程师，名叫张三")
```

## 六、RAG相关API

LangChain提供了完整的检索增强生成（RAG）工具链，覆盖从文档加载到检索生成的全流程。

### 6.1 核心组件

| 组件 | 功能 | 常用实现 |
|------|------|----------|
| `DocumentLoader` | 加载各种格式的文档 | `PyPDFLoader`、`TextLoader`、`WebBaseLoader` |
| `TextSplitter` | 将文档分割为小块 | `RecursiveCharacterTextSplitter` |
| `Embeddings` | 将文本转换为向量 | `OpenAIEmbeddings`、`HuggingFaceEmbeddings` |
| `VectorStore` | 存储和检索向量 | `Chroma`、`FAISS`、`Pinecone` |
| `Retriever` | 根据查询检索相关文档 | `VectorStoreRetriever` |

**完整RAG流程示例**：

```python
from langchain_community.document_loaders import WebBaseLoader
from langchain_text_splitters import RecursiveCharacterTextSplitter
from langchain_openai import OpenAIEmbeddings
from langchain_community.vectorstores import Chroma

# 1. 加载文档
loader = WebBaseLoader("https://python.langchain.com/docs/concepts/lcel/")
documents = loader.load()

# 2. 分割文档
text_splitter = RecursiveCharacterTextSplitter(
    chunk_size=1000,
    chunk_overlap=200
)
splits = text_splitter.split_documents(documents)

# 3. 创建向量存储
embeddings = OpenAIEmbeddings()
vectorstore = Chroma.from_documents(documents=splits, embedding=embeddings)

# 4. 创建检索器
retriever = vectorstore.as_retriever(search_kwargs={"k": 3})
```

### 6.2 构建RAG链

使用LCEL构建完整的RAG问答链：

```python
from langchain_core.runnables import RunnablePassthrough
from langchain_core.output_parsers import StrOutputParser

# 定义RAG提示词
rag_prompt = ChatPromptTemplate.from_template("""
根据以下上下文回答问题：
上下文：{context}
问题：{question}
如果上下文没有相关信息，请回答"我不知道"。
""")

# 构建RAG链
rag_chain = (
    {"context": retriever, "question": RunnablePassthrough()}
    | rag_prompt
    | model
    | StrOutputParser()
)

# 调用RAG链
response = rag_chain.invoke("什么是LCEL？")
print(response)
```

## 七、记忆API

记忆系统用于存储和检索对话历史，让AI能够记住之前的对话内容。

### 7.1 核心类

| 类 | 功能 | 适用场景 |
|----|------|----------|
| `InMemoryChatMessageHistory` | 内存存储对话历史 | 开发调试、短会话 |
| `RedisChatMessageHistory` | Redis存储对话历史 | 生产环境、长会话 |
| `RunnableWithMessageHistory` | 将记忆集成到链中 | 所有需要记忆的场景 |

**示例：带记忆的对话链**

```python
from langchain_core.chat_history import InMemoryChatMessageHistory
from langchain_core.runnables.history import RunnableWithMessageHistory

# 定义聊天历史存储
store = {}

def get_session_history(session_id: str):
    if session_id not in store:
        store[session_id] = InMemoryChatMessageHistory()
    return store[session_id]

# 构建基础链
prompt = ChatPromptTemplate.from_messages([
    ("system", "你是一个有用的助手"),
    MessagesPlaceholder(variable_name="history"),
    ("human", "{question}")
])

chain = prompt | model | StrOutputParser()

# 集成记忆
chain_with_history = RunnableWithMessageHistory(
    chain,
    get_session_history,
    input_messages_key="question",
    history_messages_key="history"
)

# 调用带记忆的链
config = {"configurable": {"session_id": "user_123"}}
print(chain_with_history.invoke({"question": "我叫张三"}, config=config))
print(chain_with_history.invoke({"question": "我叫什么名字？"}, config=config))
```

## 八、代理API（LangGraph集成）

从LangChain 1.0开始，代理功能已全面迁移至**LangGraph**，使用统一的`create_agent()`函数构建生产级智能体。

### 8.1 基本ReAct代理

```python
from langgraph.prebuilt import create_react_agent
from langgraph.checkpoint.memory import MemorySaver

# 定义工具
tools = [add, multiply]

# 创建记忆
memory = MemorySaver()

# 创建ReAct代理
agent = create_react_agent(
    model,
    tools,
    checkpointer=memory
)

# 运行代理
config = {"configurable": {"thread_id": "1"}}
result = agent.invoke(
    {"messages": [("human", "3加5等于多少？再乘以2是多少？")]},
    config=config
)

# 输出最终结果
print(result["messages"][-1].content)
```

### 8.2 流式输出代理执行过程

```python
# 流式输出代理的所有事件
for event in agent.stream_events(
    {"messages": [("human", "3加5等于多少？再乘以2是多少？")]},
    config=config,
    version="v3"
):
    if event["event"] == "on_chat_model_stream":
        print(event["data"]["chunk"].content, end="", flush=True)
    elif event["event"] == "on_tool_start":
        print(f"\n调用工具：{event['name']}，参数：{event['data']['input']}")
    elif event["event"] == "on_tool_end":
        print(f"工具结果：{event['data']['output']}")
```

## 九、中间件系统（2026年重要新增）

LangChain 1.3引入了强大的中间件系统，允许在代理执行的各个阶段注入自定义逻辑：

### 9.1 内置中间件

```python
from langchain.middleware import (
    PIIMiddleware, 
    SummarizationMiddleware, 
    HumanInTheLoopMiddleware
)

# 创建带中间件的代理
agent = create_react_agent(
    model,
    tools,
    middleware=[
        PIIMiddleware(),  # 自动检测和屏蔽PII信息
        SummarizationMiddleware(max_tokens=4000),  # 自动摘要长上下文
        HumanInTheLoopMiddleware(require_approval=["sensitive_action"])  # 敏感操作需要人工确认
    ]
)
```

### 9.2 自定义中间件

```python
def log_middleware(handler, request, run_id):
    """日志中间件示例"""
    print(f"开始执行：{request['name']}")
    result = handler(request)
    print(f"执行完成：{request['name']}")
    return result

def retry_middleware(handler, request, run_id):
    """重试中间件示例"""
    max_retries = 3
    for attempt in range(max_retries):
        try:
            return handler(request)
        except Exception as e:
            if attempt == max_retries - 1:
                raise e
            print(f"重试 {attempt + 1}/{max_retries}: {e}")

# 使用自定义中间件
agent = create_react_agent(
    model,
    tools,
    middleware=[log_middleware, retry_middleware]
)
```

## 十、生产级工具链（2026年完整生态）

### 10.1 LangSmith - 可观测性与评估平台

LangSmith是LangChain官方的生产监控和调试平台：

```python
import os
from langsmith import Client

# 设置LangSmith
os.environ["LANGCHAIN_TRACING_V2"] = "true"
os.environ["LANGCHAIN_API_KEY"] = "your-langsmith-api-key"

# 自动追踪所有链的执行
chain = prompt | model | parser
result = chain.invoke(input_data)  # 自动记录到LangSmith

# 手动创建数据集进行评估
client = Client()
dataset = client.create_dataset("math_questions")
client.create_example(
    inputs={"question": "2+2=?"},
    outputs={"answer": "4"},
    dataset_id=dataset.id
)

# 批量评估
from langsmith.evaluation import evaluate

def predict(inputs: dict) -> dict:
    return {"answer": chain.invoke(inputs)}

results = evaluate(
    predict,
    data=dataset,
    evaluators=[accuracy_evaluator]
)
```

### 10.2 LangServe - REST API部署

将任何Runnable部署为生产就绪的REST API：

```python
# server.py
from fastapi import FastAPI
from langserve import add_routes
from langchain_openai import ChatOpenAI
from langchain_core.prompts import ChatPromptTemplate

app = FastAPI(
    title="LangChain Server",
    version="1.0",
    description="A simple API server using LangChain's Runnable interfaces",
)

# 创建链
model = ChatOpenAI(model="gpt-4o")
prompt = ChatPromptTemplate.from_template("告诉我关于{topic}的有趣事实")
chain = prompt | model

# 添加路由
add_routes(
    app,
    chain,
    path="/facts",
)

if __name__ == "__main__":
    import uvicorn
    uvicorn.run(app, host="localhost", port=8000)
```

启动服务后，可通过HTTP调用：

```bash
curl -X POST http://localhost:8000/facts/invoke \
  -H "Content-Type: application/json" \
  -d '{"input": {"topic": "量子计算"}}'
```

### 10.3 LangGraph Platform - Agent部署运行时

LangGraph Platform提供企业级Agent部署和管理：

```python
# platform_agent.py
from langgraph.platform import deploy_agent
from langgraph.prebuilt import create_react_agent

# 创建代理
agent = create_react_agent(model, tools)

# 部署到LangGraph Platform
deployment = deploy_agent(
    agent,
    name="math-assistant",
    version="1.0",
    description="一个能解决数学问题的智能助手",
    permissions={
        "public": False,
        "allowed_users": ["user123", "admin"]
    }
)

print(f"Agent deployed at: {deployment.url}")
```

### 10.4 LangMem - 长期记忆SDK

LangMem提供跨会话的长期记忆能力：

```python
from langmem import LongTermMemory

# 初始化长期记忆
ltm = LongTermMemory(
    memory_type="vector",  # 向量记忆
    embedding_model="text-embedding-3-large"
)

# 保存记忆
ltm.remember(
    user_id="user123",
    content="用户喜欢讨论量子物理和人工智能",
    tags=["interests", "physics", "ai"]
)

# 查询相关记忆
relevant_memories = ltm.recall(
    user_id="user123",
    query="用户的兴趣爱好",
    limit=3
)
```

## 十一、错误处理与可靠性

### 11.1 重试与降级策略

```python
# 重试失败的调用
retry_chain = chain.with_retry(
    stop_after_attempt=3,
    wait_exponential_jitter=True
)

# 降级策略 - 主模型失败时使用备用模型
backup_model = ChatOpenAI(model="gpt-3.5-turbo")
fallback_chain = chain.with_fallbacks([lambda x: backup_model.invoke(x)])
```

### 11.2 超时控制

```python
# 设置超时
timeout_chain = chain.with_config(
    {"run_name": "timeout_chain"},
    {"callbacks": [TimeoutCallback(timeout_seconds=30)]}
)
```

## 十二、已弃用的API（重要）

以下API在LangChain 1.0+中已被弃用，请勿在新项目中使用：

| 已弃用API | 替代方案 |
|-----------|----------|
| `LLMChain` | LCEL（`prompt | model | parser`） |
| `ConversationChain` | `RunnableWithMessageHistory` |
| `initialize_agent()` | `langgraph.prebuilt.create_react_agent()` |
| `AgentExecutor` | LangGraph编译后的图 |
| `ConversationBufferMemory` | `BaseChatMessageHistory` |

## 十三、最佳实践与性能优化

### 13.1 模型选择指南

| 场景 | 推荐模型 | 理由 |
|------|----------|------|
| 通用任务 | GPT-4o | 平衡性能、成本和功能 |
| 复杂推理 | Claude Opus 4.7 | 最强推理能力 |
| 低成本 | GPT-3.5-turbo | 经济实惠 |
| 本地部署 | Llama 4 | 开源可私有化 |
| 中文优化 | DeepSeek-V4 | 中文理解优秀 |

### 13.2 性能优化技巧

```python
# 1. 批量处理提高吞吐量
results = chain.batch(list_of_inputs)

# 2. 异步并发处理
import asyncio
async def process_concurrently():
    tasks = [chain.ainvoke(input) for input in inputs]
    return await asyncio.gather(*tasks)

# 3. 缓存重复请求
from langchain.cache import InMemoryCache
langchain.llm_cache = InMemoryCache()
```

## 十四、高级特性（2026年新增）

### 14.1 MCP（Model Context Protocol）支持

MCP是2026年新兴的标准协议，用于统一模型上下文管理。LangChain通过`langchain-mcp`包提供原生支持：

```python
from langchain_mcp import MCPClient, MCPTool

# 创建MCP客户端
mcp_client = MCPClient(
    endpoint="https://mcp.example.com",
    api_key="your-mcp-key"
)

# 注册MCP工具
mcp_tools = mcp_client.get_tools()
agent = create_react_agent(model, mcp_tools)
```

MCP支持的功能包括：

- **资源订阅**：动态获取外部数据源
- **上下文同步**：跨会话保持上下文一致性  
- **权限委托**：安全的第三方工具访问

### 14.2 多模态处理

LangChain 1.3+ 原生支持多模态输入处理：

```python
from langchain_core.messages import HumanMessage
from langchain_openai import ChatOpenAI

# 处理图像+文本输入
model = ChatOpenAI(model="gpt-4o")

message = HumanMessage(
    content=[
        {"type": "text", "text": "描述这张图片的内容"},
        {"type": "image_url", "image_url": {"url": "https://example.com/image.jpg"}}
    ]
)

response = model.invoke([message])
```

支持的多模态场景：

- **图像理解**：GPT-4o、Claude 3 Opus
- **文档解析**：PDF、Word、Excel内容提取
- **音频转录**：集成Whisper等语音模型

### 14.3 评估与测试框架

LangChain提供完整的评估体系：

```python
from langchain.evaluation import load_evaluator
from langchain.smith import RunEvalConfig

# 加载评估器
evaluator = load_evaluator("qa")

# 配置自动化评估
eval_config = RunEvalConfig(
    evaluators=["qa", "relevancy", "faithfulness"],
    custom_evaluators=[custom_security_evaluator]
)

# 运行评估
results = evaluate(
    rag_chain,
    data=evaluation_dataset,
    evaluator_config=eval_config
)
```

评估维度包括：

- **准确性**：答案是否正确
- **相关性**：回答是否相关
- **忠实度**：是否基于提供的上下文
- **安全性**：是否包含有害内容
- **成本效率**：token使用和响应时间

## 十五、总结

LangChain 1.3 API的核心设计理念是**模块化、统一化和生产就绪**：

- 所有组件都实现了Runnable协议，支持统一的调用接口
- LCEL提供了简洁优雅的声明式语法来构建复杂链
- LangGraph作为编排层，完美支持循环、状态管理和人机交互
- 全面的工具、记忆和RAG支持，覆盖绝大多数AI应用场景
- 完整的生态系统（LangServe、LangSmith、LangGraph Platform）支持端到端开发
- 新兴协议支持（MCP）和多模态能力确保技术前瞻性

**核心公式**：

```
LangChain应用 = 提示词 + 模型 + 工具 + 记忆 + 检索
            = 多个Runnable组件通过LCEL组合
            = LangGraph状态图编排
            + LangServe部署 + LangSmith监控
            + MCP扩展 + 多模态支持
```

**生产环境推荐架构**：

```
用户界面 → LangServe API → LangGraph Agent → 
    ├── LangSmith监控与评估
    ├── Checkpointer持久化存储
    ├── 中间件安全层（PII、权限、速率限制）
    ├── MCP协议扩展工具集
    └── 多模型提供商自动切换
```

需要我提供一个**集成了RAG、工具调用、记忆、LangServe部署、LangSmith监控、MCP支持和多模态处理的完整智能体代码模板**，可以直接运行吗？
