# Token（令牌）技术全面解析

Token是**大语言模型（LLM）处理和生成文本的最基本单位**，是连接人类自然语言与AI数字世界的桥梁。理解Token是掌握大模型工作原理、优化提示词、控制推理成本和上下文长度的核心基础。

## 一、基本定义与核心本质

### 1. 核心定义

Token是大模型对文本进行**"语义切分"**后得到的离散符号单元。它既不是单个字符，也不是完整的单词，而是介于两者之间的、经过统计学习得到的"语义原子"。

简单来说：大模型**不认识字，也不认识词，只认识Token**。所有输入给大模型的文本都会先被转换为Token序列，所有大模型输出的内容也都是Token序列。

### 2. 为什么不用"字"或"词"作为基本单位？

这是大模型发展过程中经过反复验证的最优选择，解决了三大核心问题：

| 方案 | 核心问题 | 示例 |
|------|----------|------|
| 按字符切分 | 序列过长，计算效率极低；无法捕捉语义 | "人工智能"被切分为4个独立字符，丢失整体语义 |
| 按单词切分 | 词汇量爆炸；无法处理未登录词（OOV） | 无法识别新词"ChatGPT"、组合词"元宇宙"、拼写错误 |
| 按Token切分 | 平衡了序列长度和语义完整性；解决未登录词问题 | "ChatGPT"被切分为`["Chat", "G", "PT"]`，保留语义 |

### 3. 与Embedding的关系

- **Token是离散的符号ID**：本质是一个整数，如`"人工智能" = 12345`
- **Embedding是连续的向量表示**：将Token ID映射为固定维度的稠密向量，如`12345 = [0.123, -0.456, ...]`
- **完整流程**：`文本 → Token序列 → Embedding序列 → 大模型处理`

## 二、Token的生成原理：分词算法（Tokenizer）

Token不是人为规定的，而是通过**无监督统计学习**从海量文本中自动学习得到的。目前主流大模型几乎都采用**字节对编码（Byte Pair Encoding, BPE）**算法。

### 1. BPE算法核心思想

从最基本的字符开始，反复合并语料中**出现频率最高的相邻字符对**，直到达到预设的词汇表大小。

### 2. BPE训练过程示例

假设我们有语料：`"low low low new new new year year"`

1. **初始词汇表**：所有单个字符：`l, o, w, n, e, y, a, r`
2. **统计相邻字符对频率**：`lo:3, ow:3, ne:3, ew:3, ye:2, ar:2`
3. **合并最高频对**：合并`lo`为新Token`lo`，词汇表更新
4. **重复迭代**：继续合并`low`、`ne`、`new`等，直到词汇表达到目标大小

### 3. 主流分词算法对比

| 算法 | 代表模型 | 核心特点 | 优势 | 劣势 |
|------|----------|----------|------|------|
| BPE | GPT系列、LLaMA系列、Qwen系列 | 合并高频字节对 | 实现简单，推理速度快，词汇表可控 | 分词结果不唯一，可能切分语义单元 |
| WordPiece | BERT系列、早期Google模型 | 基于似然概率合并 | 更倾向于保留完整语义单元 | 训练复杂，速度较慢 |
| Unigram | T5、XLNet | 基于子词概率模型 | 分词结果更稳定，支持多种切分方式 | 计算复杂度高 |

## 三、Token的类型与结构

### 1. 普通Token

- **子词Token**：最常见的类型，如`"unhappy"` → `["un", "happy"]`
- **完整单词Token**：高频常用词会作为一个完整Token，如`"the"`、`"hello"`
- **字符Token**：生僻字、特殊符号会被切分为单个字符
- **字节Token**：对于完全未见过的字符，会 fallback 到UTF-8字节表示

### 2. 特殊Token

特殊Token是大模型的"控制指令"，不对应任何自然语言文本，但对模型的行为至关重要。

| 特殊Token | 作用 | 代表模型 |
|-----------|------|----------|
| `<|endoftext|>` | 文本结束标记，标志输入或输出的结束 | GPT系列 |
| `<|beginoftext|>` | 文本开始标记 | GPT-2、LLaMA |
| `[CLS]` | 分类标记，用于句子级分类任务 | BERT系列 |
| `[SEP]` | 分隔标记，用于分隔两个句子 | BERT系列 |
| `<|im_start|>` | 对话开始标记，标志角色（系统/用户/助手） | Qwen、GPT-3.5-turbo |
| `<|im_end|>` | 对话结束标记 | Qwen、GPT-3.5-turbo |
| `<|padding|>` | 填充标记，用于将不同长度的序列补全到相同长度 | 几乎所有模型 |

### 3. 词汇表（Vocabulary）

词汇表是所有Token的集合，每个Token对应一个唯一的整数ID。

- 主流大模型词汇表大小：32000（LLaMA-1）、50257（GPT-2）、128000（GPT-4、Qwen3）
- 词汇表大小直接影响模型的参数量和推理效率：词汇表越大，Embedding层参数量越大，但序列长度越短

## 四、Token的量化指标与换算关系

### 1. 通用换算比例（2026年主流模型）

这是实际应用中最常用的经验值，直接影响API计费和上下文窗口计算：

| 语言 | 换算关系 | 示例 |
|------|----------|------|
| 中文 | 1个Token ≈ 1.3-1.5个汉字 | 1000个汉字 ≈ 667-769个Token |
| 英文 | 1个Token ≈ 0.75个单词 ≈ 4个字符 | 1000个英文单词 ≈ 1333个Token |
| 代码 | 1个Token ≈ 1-1.2个代码行 | 100行代码 ≈ 83-100个Token |

**重要提示**：标点符号、空格、换行符都会被算作Token。例如，一个换行符`\n`就是一个独立的Token。

### 2. 上下文窗口（Context Window）

大模型的上下文窗口大小**永远是指Token数量，而不是字符数或单词数**。

- 例如：GPT-4o的128K上下文窗口，意味着它最多能处理约17万汉字或9.6万英文单词
- 上下文窗口包含：输入提示词 + 模型输出内容

### 3. Token与成本的关系

几乎所有大模型API都按Token计费，计费单位通常是"每千Token"。

- 输入Token和输出Token价格不同，输出Token通常更贵（约为输入的2-3倍）
- 例如：GPT-4o输入价格为$0.005/千Token，输出价格为$0.015/千Token

## 五、Token在大模型中的完整生命周期

1. **输入分词**：用户输入的文本被Tokenizer转换为Token ID序列

   ```
   "你好，世界" → [10842, 3647, 11, 1938]
   ```

2. **Embedding映射**：每个Token ID被转换为对应的Embedding向量

   ```
   10842 → [0.123, -0.456, 0.789, ...]（1536维向量）
   ```

3. **模型推理**：Transformer模型对Embedding序列进行处理，预测下一个Token的概率分布

4. **采样生成**：从概率分布中采样出下一个Token ID

   ```
   预测下一个Token：1234（对应"！"）
   ```

5. **解码输出**：将生成的Token ID序列转换回人类可读的文本

   ```
   [10842, 3647, 11, 1938, 1234] → "你好，世界！"
   ```

6. **循环迭代**：将生成的Token加入上下文，重复步骤3-5，直到生成结束标记`<|endoftext|>`

## 六、主流大模型Tokenizer对比（2026年）

| 模型系列 | 分词算法 | 词汇表大小 | 中文支持 | 特殊Token | 特点 |
|----------|----------|------------|----------|-----------|------|
| GPT-4o | BPE | 128000 | 优秀 | `<|endoftext|>`、`<|im_start|>` | 多语言支持最好，中文分词效率高 |
| LLaMA 3 | BPE | 128256 | 良好 | `<|end_of_text|>`、`<|eot_id|>` | 开源标杆，词汇表包含大量代码Token |
| Qwen 3 | BPE | 128000 | 极佳 | `<|endoftext|>`、`<|im_start|>` | 中文分词最准确，支持大量中文成语 |
| BERT-base | WordPiece | 30522 | 一般 | `[CLS]`、`[SEP]` | 仅支持中英文，适合分类任务 |

## 七、核心应用场景与实用技巧

### 1. API成本控制

- 精简提示词：删除不必要的空格、换行和冗余描述
- 使用缩写：在不影响语义的前提下使用缩写
- 批量处理：将多个小请求合并为一个大请求，减少Token overhead

### 2. 上下文管理

- 计算上下文长度：使用官方Tokenizer工具准确计算输入Token数
- 截断长文本：对于超过上下文窗口的文本，只保留最相关的部分
- 滑动窗口：处理超长文档时，使用滑动窗口逐段处理

### 3. 提示词优化

- 避免Token浪费：不要使用大量重复的词语
- 利用特殊Token：正确使用对话标记符，帮助模型理解角色和上下文
- 结构化输出：要求模型使用JSON等结构化格式，减少无效输出Token

### 4. 实用工具

- OpenAI Tokenizer：<https://platform.openai.com/tokenizer>
- Hugging Face Tokenizers：<https://huggingface.co/spaces/Xenova/the-tokenizer-playground>
- 各模型官方提供的Tokenizer库

## 八、常见误区与局限性

### 1. 常见误区

- ❌ 误区1："1个汉字等于1个Token"
  ✅ 正确：中文平均1.3-1.5个汉字等于1个Token，生僻字可能需要多个Token
- ❌ 误区2："不同模型的Token是通用的"
  ✅ 正确：每个模型都有自己独立的Tokenizer和词汇表，Token ID不能跨模型使用
- ❌ 误区3："上下文窗口大小是字符数"
  ✅ 正确：所有大模型的上下文窗口都是指Token数量
- ❌ 误区4："空格和标点不算Token"
  ✅ 正确：所有可见和不可见字符（包括空格、换行、制表符）都会被算作Token

### 2. 核心局限性

1. **语义切分不合理**：有时会将完整的语义单元切分开，影响模型理解
   例如："北京大学"可能被切分为`["北京", "大学"]`，但"北京大学"作为一个整体有特定含义
2. **中文分词效率低**：中文没有天然的分词边界，分词错误率高于英文
3. **多语言混合问题**：不同语言的Token密度差异大，混合文本会导致上下文窗口利用率降低
4. **安全漏洞**：恶意用户可以构造特殊的Token序列，触发模型的安全漏洞
5. **不可解释性**：Token的切分逻辑是统计学习的结果，难以解释为什么某个词会被这样切分

## 九、未来发展趋势

1. **更大的词汇表**：随着模型能力提升，词汇表将继续扩大，减少子词切分，提高语义完整性
2. **多模态统一Token**：实现文本、图像、音频、视频的统一Token表示，构建多模态大模型的基础
3. **动态Token长度**：根据语义复杂度动态调整Token长度，重要语义用更长的Token表示
4. **可解释Tokenizer**：研究具有明确语义含义的Token，提升模型的可解释性
5. **低资源语言支持**：优化Tokenizer对低资源语言的处理能力，减少语言偏见

## 总结

Token是大模型的"语言单位"，Tokenizer是大模型的"翻译官"。所有大模型的能力都是建立在对Token序列的处理之上。

理解Token不仅能帮助你更好地使用大模型，优化提示词，控制成本，还能让你深入理解大模型的工作原理。在实际应用中，记住三个核心要点：

1. 大模型只认识Token，不认识字或词
2. 上下文窗口和API计费都是按Token计算的
3. 不同模型的Tokenizer不通用
