# 什么是CLI

CLI(Command-Line Interface)是指**命令行界面**,在命令行输入命令，程序可以感知输入的内容并进行相应的程序执行。用python举例：

```python
# 假设有个add.py执行文件，后面跟参数，你通过CLI是可以直接执行的，注意需要add.py文件可以处理传入参数并进行后续功能运行。
python add.py 3 5
# 输出: 结果: 8.0
```
