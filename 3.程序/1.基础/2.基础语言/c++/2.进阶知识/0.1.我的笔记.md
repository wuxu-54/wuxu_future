# 我的笔记

## 解决CMakeList配置的宏VS code显示红色波浪线问题

windows：

VSCode：`Ctrl+Shift+P` →`C/C++: Edit Configurations (UI)`->`Compile commands`填：

    ```
    ${workspaceFolder}/build/compile_commands.json
    ```
