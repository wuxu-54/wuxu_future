# 学习前讲两句

文本命令有很多，但其侧重不同，要注意。

1. 不用背命令，可以通过 `[tab]`补全、`[tab][tab]`提示、`man`、`info`、`--help` 等方式去了解你需要的命令的作用
2. 可以使用本笔记找到对应分类，随用随查。
3. script脚本执行,`sh 脚本` 单开线程执行，`source 脚本`跟控制台直接执行命令相同线程。
4. `test` 可以做命令的测试验证。
